<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
  <% console.log(userName) %>
  <% if(!userName){ %>
    <form method="POST" action="/login">
      <label>Username</label>
      <input type="input" name="usernameField"  pattern="[A-Za-z\s]+"maxlength="100" minlength="0">
      <input type="submit" value="Login In"> %>
    </form>
  <% } else { %>
    <form method="POST" action="/logout">
      <input type="submit" value="Logout">
    </form>
  <% } %> <!--set two <form> in different conditions> -->

</body>
</html>
